Vimes
=====

Vimes is a simplistic list manager that allows users to edit and create
lists within a web browser. The basic goal is to have a simple way to 
manage lists from a desktop browser and then be able to update them from
a modern smartphone (iPhone or Android).

Installation
============

Vimes has the following requirements:
* Apache/WSGI Web server
* Python
* Flask
* mySQLdb

The database can be initialized by setting the correct values in the
config.py file and then in a python shell:
 import vimes
 init_db()
